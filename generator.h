#ifndef GENERATOR_H
#define GENERATOR_H

#include <string>
#include <vector>
#include <random>
#include <algorithm>

std::string generator(int length, bool num, bool symbol, bool upper, bool lower, std::string begin);

#endif
